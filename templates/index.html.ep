% stash('template', __FILE__);
% layout 'default';
%= tag h1 => 'Login'
%= include '_flash'

<div class="cellblue">

%= form_for login => (method => 'post') => begin
    %= include '_credentials'
    %= tag 'br'
    %= submit_button 'Login', style => "border: 1px solid #bbb;"
%= end

</div>
